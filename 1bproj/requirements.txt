# === Required Python Libraries ===
# These packages are needed to run the document intelligence system
# for Round 1B of the "Persona-Driven Document Intelligence" challenge.

# PDF reading and text extraction
PyMuPDF==1.22.3

# Text analysis and scoring
scikit-learn==1.3.0

# Fix: Explicit version to avoid import issues with ComplexWarning
numpy==1.24.4

# (Optional but useful) — natural language toolkit for enhancements later
nltk==3.8.1

# Note: JSON and datetime are built-in to Python — no need to list
